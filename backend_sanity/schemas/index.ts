import about from './about'
import experiences from './experiences'
import projects from './projects'

export const schemaTypes = [
    // When you import schemas, the order matters
    about, experiences, projects

]
